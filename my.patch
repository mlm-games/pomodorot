--- a/data/main.gd
+++ b/data/main.gd
@@ -8,6 +8,7 @@
 @onready var stop_button: Button = %StopButton
 @onready var skip_button: Button = %SkipButton
 
+@onready var modal_overlay: ColorRect = %ModalOverlay
 @onready var counter_label: Label = %CounterLabel
 
 var notification_manager: Node
@@ -124,7 +125,8 @@
 
 func _on_settings_button_pressed() -> void:
 	var settings_dialog := preload("res://data/settings_dialog.tscn").instantiate()
+	settings_dialog.overlay_node = modal_overlay # Pass the overlay node
 	add_child(settings_dialog)
-	settings_dialog.popup_centered()
+	settings_dialog.popup_animated() # Use our new animated popup function
 
 func _notification(what: int) -> void:
 	match what:

